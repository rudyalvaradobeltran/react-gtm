import styled from 'styled-components';

const Styles = styled.div`
  img {
    border-radius: 15px;
    object-fit: cover;
    width: 100%;
  }
  .image-link:hover {
    cursor: pointer;
  }
`;

export default Styles;