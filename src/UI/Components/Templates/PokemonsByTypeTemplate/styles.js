import styled from 'styled-components';

const Styles = styled.div`
  .box-pokemons-by-type {
    height: calc(100vh - 100px);
    display: flex;
    align-items: center;
    padding-left: 200px;
    padding-right: 200px;
  }
`;

export default Styles;