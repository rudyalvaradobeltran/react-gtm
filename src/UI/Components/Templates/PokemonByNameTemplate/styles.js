import styled from 'styled-components';

const Styles = styled.div`
  .box-pokemon-by-name {
    height: calc(100vh - 100px);
    display: flex;
    align-items: center;
    padding-left: 200px;
    padding-right: 200px;
  }
  .grid-pokemon-by-name {
    justify-content: center;
    display: flex;
  }
`;

export default Styles;